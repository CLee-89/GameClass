1.
While true
	create a boolean isLower and set to true
	ask user to input a phrase with lowercase letters only
	phrase = input

	for each loop for each character in phrase
		if char is not >= 'a' and <= 'z'  
			tell user to use lowercase alphabet only
			set isLower to false 
			break out of loop
	End for each loop

	if isLower = true then break out of loop
End while

for each loop for each character in phrase
	char = char - 32      // Any lower case - 32 = uppercase	
End for each loop

output phrase
-------------------------------------------------------------------------------------------
2.
While true
	ask user to input number
	a = input
	ask user for another number
	b = input
	if needed try checking if inputs are numbers and catch the error saying "Please enter a number"
		continue to the next loop iteration if error occurred
End while

c = a + b
output c
-------------------------------------------------------------------------------------------
3.
While true
  Ask user how many numbers they want to enter
  numbers = input
  try checking if numbers is a number, if not catch the error and display "Please enter a valid number" and continue to next loop

  create sum = 0

  for loop from 0 to numbers - 1
    Ask user to input number
    try checking if number is a number, if not catch the error and display "Please enter a valid number", subtract 1 from loop iteration and continue to next loop.
    sum = sum + input
  break out of loop
  End for loop

End While

Output sum
-------------------------------------------------------------------------------------------
4.
create boolean continue and set to true
While continue is true
	Ask user to define a maximum number for a random number being generated
	max = input
	try checking if number is a whole number, if not catch the error and display "Please enter a valid whole number" and continue to next loop
	generate random number using input as maximum value, or maximum value + 1 if language goes from 0 to maximum - 1
	number = random number generated
	output number

	while true
		ask user to if they want to enter new number, input y for yes or n for no
		if input = 'n' or 'N'
			set continue to false
			break out of loop
		else if input = 'y' or 'Y'
			break out of loop
		else 
			tell user their input must be a valid input
	End while

End While
-------------------------------------------------------------------------------------------
5.
create an integer called leftCount = 0
create an integer called rightCount = 0

for loop from 0 to 9
	generate a random number from 1 - 100
	if the number mod 2 = 0
		print "RIGHT"
		add 1 to rightCount
	else
		print "LEFT"
		add 1 to leftCount
End For Loop

print "left:" leftCount
print "right:" rightCount

